# Local development: run npx convex dev --once, then use its generated URL.
VITE_CONVEX_URL=
# Configure on your Convex deployment, not as VITE_ variables:
# npx convex env set ANTHROPIC_API_KEY <your-key>
# npx convex env set OPENUI_TELEMETRY_DISABLED 1
# Optional: npx convex env set ANTHROPIC_MODEL claude-sonnet-4-6
